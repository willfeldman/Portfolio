{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": {
                "npmInstallCommand": "npm install --legacy-peer-deps"
            }
        }
    ]
}